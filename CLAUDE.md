# pvfit5 — Istruzioni per lo sviluppo

## Progetto

Stima dei 5 parametri del modello a singolo diodo (SDM) di moduli fotovoltaici partendo dai soli dati di targa commerciali. Algoritmo pubblicato su Energy Reports (DOI: 10.1016/j.egyr.2026.109280).

## Convenzioni codice

- Lingua: tutto in inglese (codice, commenti, output, documentazione)
- Licenza: `SPDX-License-Identifier: BSD-3-Clause` in ogni file sorgente
- Citazione articolo obbligatoria in documentazione e output

## Regole imperative

### Nessun riferimento AI

**NON AGGIUNGERE MAI** alcun riferimento a Claude, Claude Code, Anthropic, o qualsiasi strumento AI in:
- codice sorgente, commenti, docstring
- documentazione (README, docs/)
- metadati (pyproject.toml, CITATION.cff)
- messaggi di commit, pull request
- qualsiasi artefatto pubblicato su GitHub o PyPI

**Unico autore e maintainer: Valerio Lo Brano.**

Nessuna riga `Co-Authored-By`, `Generated by`, badge o link a tool AI. **Regola assoluta, senza eccezioni.**

### Aggiornamento continuo di CLAUDE.md

Dopo ogni modifica significativa al progetto, **aggiornare questo file** con:
- Nuovi dettagli tecnici rilevanti (struttura, dipendenze, configurazioni)
- Variazioni a convenzioni, workflow, o regole di pubblicazione
- Lezioni apprese da errori (es. URL relativi vs assoluti su PyPI)
- Nuovi file o cartelle con ruolo specifico

Obiettivo: chi legge CLAUDE.md deve avere un quadro completo e aggiornato del progetto senza dover ricostruire la storia dai commit.

## Pubblicazione

| Dato | Valore |
|------|--------|
| GitHub | https://github.com/valeriolobrano/pvfit5 |
| Branch | `master` (NON `main`) |
| PyPI | https://pypi.org/project/pvfit5/ |
| Build | hatchling (`pyproject.toml`) |
| Upload | `rm -rf dist/ && python -m build && twine upload dist/*` |
| Token PyPI | `~/.pypirc` |

### Bump versione

Aggiornare **sempre tutti e 3** contemporaneamente:

1. `pyproject.toml` → `version`
2. `pvfit5/__init__.py` → `__version__` e `__date__`
3. `README.md` → riga `**Version:** X.Y.Z | **Date:** ...`

### Link e immagini nel README per PyPI

PyPI non serve file dal repo. **Tutti i link a file del repo** (immagini, PDF, ecc.) devono usare URL assoluti con branch `master`:

- Immagini: `https://raw.githubusercontent.com/valeriolobrano/pvfit5/master/images/nome.png`
- PDF/file: `https://github.com/valeriolobrano/pvfit5/raw/master/path/file.pdf`

MAI path relativi — funzionano su GitHub ma non su PyPI.

## Funzione obiettivo (GA)

La funzione obiettivo del GA è una somma pesata di errori relativi:

```
E = 5·e_Voc + e_Isc + e_Vmp + 15·e_Imp + 0.3·stationarity_residual
```

Include Vmp e Imp individualmente (non solo Pmp). Il vincolo di stazionarietà verifica che dP/dV ≈ 0 al Vmp del datasheet.

Graph 2 (plot finale) mostra sia i punti datasheet (cerchi blu) che il MPP fittato (quadrato verde) per evidenziare eventuali discrepanze.

## Struttura repository

| Cartella/File | Contenuto |
|---------------|-----------|
| `pvfit5/` | Pacchetto Python principale |
| `images/` | Immagini per README (I-V curve, live adaptation) |
| `paper/` | PDF articolo Open Access (CC-BY) |
| `example_output/` | Esempio output testuale |
| `CITATION.cff` | Metadati citazione machine-readable |
